name: ${PROJECT_NAME}
services:
  firstservice:
    image: firstservice
    build:
      context: .
      dockerfile: FirstService/Dockerfile
    env_file: .env
    deploy:
      replicas: 5
    ports:
      - "${NODE_MIN_PORT}-${NODE_MAX_PORT}:8080"